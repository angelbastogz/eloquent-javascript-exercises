import { ArrayFormattingPipe } from './array-formatting.pipe';

describe('ArrayFormattingPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFormattingPipe();
    expect(pipe).toBeTruthy();
  });
});
